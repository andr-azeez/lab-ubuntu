apiVersion: 1.0.0
metadata:
  name: wksp-ubuntu
components:
  - mountSources: true
    command:
      - tail
    args:
      - /dev/null
      - '-f'
    memoryLimit: 256Mi
    type: dockerimage
    image: quay.io/libpod/ubuntu
    alias: ubuntu
